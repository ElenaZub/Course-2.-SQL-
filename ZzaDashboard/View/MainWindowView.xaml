<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Data="clr-namespace:Zza.Data;assembly=Zza.Data"
             xmlns:local="ZzaDashboard.ViewModel"
             x:Class="ZzaDashboard.View.CustomerEditView"
             mc:Ignorable="d" Height="Auto" Width="Auto">
    <UserControl.DataContext>
        <local:MainWindowsViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.9*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*"/>
            <ColumnDefinition Width="0.9*"/>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" ItemsSource="{Binding Customers}">

        </DataGrid>
        <Button Grid.Column="0" Grid.Row="1" Margin="5" Content="Get" Command="{Binding GetCommand}"/>
    </Grid>
</UserControl>
